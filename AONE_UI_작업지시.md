# Aone — UI 작업 지시서 (Claude Code용)

이 문서 전체가 하나의 작업 지시입니다. **실행 프로토콜**을 먼저 읽고 그대로 따르세요.

---

## 실행 프로토콜

### 0단계 · 조사 후 계획 보고 (코드 수정 금지)

1. 이 문서 전체를 읽는다.
2. 각 FIX에 대해 **관련 파일 경로**를 실제로 검색해 확인한다. 추정하지 마라.
3. 각 FIX의 "먼저 조사할 것"(파일 탐색, SQLite 조회 등)을 수행하고 결과를 보고한다.
4. 아래 형식으로 계획표를 제출한다.

```
| # | 작업 | 관련 파일 | 조사 결과 (원인/현재 상태) | 예상 변경 범위 | 리스크 |
```

5. **여기서 멈춘다.** 승인 없이 어떤 파일도 수정하지 않는다.

### 1단계 · 한 번에 하나씩

- 내가 "FIX N 진행" 또는 "다음"이라고 하면 **그 작업 하나만** 수행한다.
- 여러 작업을 묶지 않는다. 다음 작업을 미리 시작하지 않는다.
- 각 작업 전 `git add -A && git commit -m "wip: before FIX N"`.
- 작업 후 dev 서버에서 **실제로 화면을 열어** 완료 기준을 확인하고 아래 형식으로 보고한다.

```
FIX N 완료 보고
- 변경한 파일:
- 완료 기준 체크:
  [O] 기준 1 — 확인한 방법
  [X] 기준 2 — 안 된 이유
  [?] 기준 3 — 확인 못 한 이유
- 부작용 확인: 학습 가이드 / 홈 / 슬라이드 리더가 이전과 동일한지
- 남은 이슈:
```

- 확인하지 않은 항목을 `[O]`로 적지 마라.
- 보고 후 **멈춘다.**

### 전 작업 공통 규칙

- 지정된 화면만 대상으로 한다. 다른 화면·라우트는 수정하지 않는다.
- 리팩터링, 의존성 추가, 파일 이동, 이름 변경 금지. 최소 diff.
- 파이프라인 로직·LLM 프롬프트·DB 스키마는 **FIX 13을 제외하고** 변경 금지.
- 하드코딩 금지. 화면 값은 전부 DB에서 나와야 한다.
- 새 색·새 스타일을 만들지 않는다. 기준은 **시험 대비 > 학습 가이드** 화면이다.
- 데이터가 없으면 그 영역을 렌더하지 않는다. 빈 박스·깨진 이미지·플레이스홀더 금지.

### 절대 건드리지 말 것

- **시험 대비 > 학습 가이드의 기존 레이아웃** — 정상 동작을 확인했다. 여기 추가하는 건 FIX 12의 근거 이미지뿐이다.
- 폴더 구조 재설계, 5탭 위치 재편, 학습 이력 저장 기능
- 디자인 시스템 통일, 컬러 토큰 정리
- AI 챗봇 실제 구현 (FIX 2는 가짜 말풍선 제거만)
- 폴더 드래그 재정렬 (FIX 15는 배지 표시만)

### 권장 순서

```
A그룹 (서사·버그)     FIX 1 → 2 → 3 → 4
B그룹 (학습 내용 밀도) FIX 11 → 12 → 13
C그룹 (저비용 정합)    FIX 5 → 6 → 7 → 8 → 9 → 10
D그룹 (여유)          FIX 14 → 15
```

---

# A그룹 — 서사와 버그

## FIX 1 — 에이전트 탭: 실행 로그 → 능동 제안 카드

대회 주제가 "라이프스타일 혁신을 위한 AI Agent"인데 '에이전트' 탭이 개발자 로그다. 능동 제안 문장은 이미 DB에 있고 로그 한 줄에 묻혀 있을 뿐이라 새로 만들 게 거의 없다.

```
[작업] 에이전트 탭 상단에 '능동 제안' 카드 섹션을 신설하고, 기존 실행 로그는 접어라.
파이프라인·생성 로직 변경 금지.

[배경]
- DB에 에이전트가 먼저 발화한 능동 제안이 저장돼 있다(L4). 예:
  "'소리통신(Data over sound)' 시험신호가 10→100으로 상승했습니다.
   기출 5회 매칭 — 예상문제 2개를 준비했어요. 지금 풀어보시겠어요?"
- 화면에는 "L4 능동 제안 5건 발동 — 푸리에 변환, 변조(modulation), …" 같은 로그 한 줄로만 나오고
  정작 문장은 어디에도 보이지 않는다.

[먼저 조사할 것]
능동 제안 원문이 들어 있는 테이블·컬럼과 총 건수를 조회해 보고하라.

[요구 사항]
0. **집계 범위 — 중요.** 에이전트 탭은 주차별 화면이 아니라 전체 화면이다.
   화면이 현재 읽는 주차 스냅샷(3주차 15건)만 쓰면 건수가 틀린다.
   전 주차 스냅샷을 합치거나 DB(proactive, 총 28건)를 직접 읽어 **전체 28건**을 모아라.
   toProactiveChip() 파서는 이미 있으니 재사용한다.
1. 에이전트 탭 최상단에 섹션 신설.
   - 제목: "에이전트가 먼저 건넨 말" + 총 건수(전체 기준)
   - 카드 1장 = 제안 1건. 내용: 제안 문장 전문 / 관련 개념 칩 / 발생 시점(주차·날짜) /
     근거 요약(기출 N회 매칭, 신호 10→100 등)
   - 최신순, 기본 5장 + "더 보기"
2. 기존 "전체 활동 이력"(L0~L4 로그)은 아래로 내리고 기본 접힘. 삭제하지 마라.
3. 부제 "모든 수업의 분석 활동을 한곳에서 확인하세요"를
   에이전트가 한 일을 사람 말로 설명하는 문장으로 바꿔라.

[완료 기준]
- 탭을 열면 첫 화면에 제안 문장 카드가 보이고 L0~L4 로그는 접혀 있다.
- 카드에 실제 DB 문장이 그대로 나온다(하드코딩 금지).
- 스크린샷 한 장에 카드 3장 이상이 잡힌다.
```

## FIX 2 — AI 챗봇 탭: 가짜 사용자 말풍선 제거

```
[작업] 파일 뷰 > AI 챗봇 탭에서 미리 박혀 있는 가짜 대화를 제거하라. 챗봇을 구현하지는 마라.

[현재] 사용자가 쓴 적 없는 파란 말풍선 "이 슬라이드에서 시험에 나올 핵심만 정리해줘"가 떠 있고,
      답변은 "AI 챗봇은 준비 중입니다…"다. 빈 상태보다 나쁘다.

[요구 사항]
1. 예시 사용자 말풍선과 그에 대한 답변 말풍선을 삭제한다.
2. 화면 중앙에 준비 중 안내만 남긴다: 아이콘 + "이 자료를 근거로 답하는 챗봇을 준비하고 있어요"
3. 입력창은 비활성 상태로 두되 placeholder는 "준비 중"으로 유지한다.
4. 탭 라벨 옆에 "준비 중" 뱃지를 단다.

[완료 기준]
- 탭 진입 시 사용자 말풍선이 하나도 없다.
- 탭 라벨에 준비 중 뱃지가 보인다.
```

## FIX 3 — 폴더(주차) 뷰 상단 배너: 펼침이 빈 채로 렌더 + 아래 영역까지 사라짐

**학습 가이드가 아니다.** 학습 가이드의 개념 펼침은 정상 동작을 확인했다.

```
[작업] 과목 > 주차 폴더 화면 상단 배너의 "N개 더 보기" 펼침 버그를 고쳐라.

[재현] 데이터통신 > 3주차 > 배너 "12개 더 보기 ↓" 클릭

[증상]
1. 접힘 상태에서는 개념 칩 3개가 "개념명 + 기출 N회 · 강조 N회"로 정상 표시된다.
2. 펼치면 개념명·뱃지가 모두 사라지고 "관련 퀴즈 보기 →" 링크만 남는다.
3. 펼침 상태 링크 개수가 12개가 아니라 2개다.
4. 펼치는 순간 배너 아래의 파일 카드 4개와 요약 줄
   ("개념 53개 정리 · 교수님 강조 43곳 · 퀴즈 8개")까지 함께 사라진다.
   → 배너 컨테이너가 펼침 상태에서 아래 영역을 덮고 있을 가능성이 있다. 이것도 확인하라.

[먼저 조사할 것]
1. 이 배너 컴포넌트 경로와, 접힘/펼침이 각각 어떤 데이터를 쓰는지.
   서로 다른 소스를 쓰고 있는지 특히 확인하라.
2. 펼침용 데이터가 (a) 비어 있는지 (b) 있는데 렌더가 안 되는지
   SQLite로 판별하고 쿼리·행 수를 근거로 보고하라.

[0단계 조사에서 확인된 원인 — 반영할 것]
- 접힘/펼침 데이터 소스는 동일하다(proactiveChips). 데이터는 정상이다.
- 근본 원인은 CSS다. 펼침 <li>가 text-white/80 on bg-white/[0.06] — 밝은 배너 위 흰 글씨라
  글자가 있는데 보이지 않는다. 다크 패널용 스타일이 남은 것이다.
- 접힘은 c.concept + c.evidence를 렌더하고, 펼침은 c.text만 렌더한다.
  펼침에서도 개념명과 기출/강조 뱃지가 함께 보여야 한다.
- 증상 4(아래 카드 사라짐)는 오버레이가 아니라, 보이지 않는 li가 세로로 쌓여 카드를 화면 밖으로
  밀어낸 것으로 추정된다. 색을 고친 뒤 재현해서 확인하라.

[하지 말 것 — 중요]
- questionIds 조건을 완화하지 마라. 15건 중 2건만 questionIds가 있는 것은 정상이다.
  예상문제를 실제로 준비한 제안에만 링크가 붙는 게 맞다. 조건을 풀면 죽은 링크 13개가 생긴다.
  이 버그는 링크 개수 문제가 아니라 보이지 않는 글씨 문제였다.

[완료 기준]
- 펼치면 개념 15개가 개념명 + 기출/강조 뱃지 + 제안 문장 형식으로 전부 보인다.
- 개념명 없이 링크만 뜨는 행이 없다.
- 펼친 상태에서도 아래 파일 카드와 요약 줄이 그대로 보인다.
- 1·2·4·5주차에서도 동일하게 동작한다.
```

## FIX 4 — 족보: 무한 로딩 + 기출 출처 확인

```
[작업] 족보 화면의 무한 로딩을 고치고, 기출 데이터의 출처를 확인해 보고하라.

[증상]
- 데이터통신 > 족보: "연도별 기출 · 0건 / 기출 자료 불러오는 중…"에서 멈춘다.
- 그 아래 "2012~2018 기출 (이미지) · 이미지 2장"의 썸네일도 회색으로 뜨지 않는다.

[먼저 조사해서 반드시 보고할 것]
1. 기출 문항이 저장된 테이블에서 연도별 문항 수를 조회하라.
2. 학습 가이드가 "기출 36문항 · 2022·2023·2025"라고 표시하는 근거 데이터가 어디서 오는지 추적하라.
3. 족보 폴더의 실제 파일은 "2018 중간·문제지", "2012 중간·문제지"다.
   화면 파일(2012·2018)과 분석에 쓰인 기출(2022·2023·2025)의 출처가 다른지 판별해 보고하라.
   다르다면 어느 경로로 들어온 데이터인지 밝혀라. 이건 코드 수정보다 먼저다.

[용어 규칙 — 확정됨. 이대로 표기하라]
- 상위 개념은 **"족보"** 로 통일한다. 족보는 형식이 여러 가지다: PDF · 스캔 이미지 · 텍스트.
- 화면에서 **"기출"이라는 단어를 단독으로 쓰지 마라.** 공식 기출 원본을 뜻하는 것으로 읽히는데,
  데이터통신의 36문항은 수강생 후기 정리물(텍스트 족보)이다.
- 족보 항목마다 형식을 표시한다: `텍스트 족보 · 36문항`, `스캔 족보 · 2장`, `PDF 족보 · 6개`
- 개념 카드의 "기출 빈출" 뱃지는 **"족보 빈출"** 로 바꾼다.
- "기출 N회 매칭" → **"족보 N회 매칭"**
- 족보 상세에 출처 한 줄을 표기한다. past_exams.json의 source/note 문구를 그대로 쓴다.

[요구 사항]
- 연도별 족보 목록의 데이터 로딩 경로(fetch/state/에러 처리)를 추적해 원인을 특정하고 고쳐라.
  0단계 조사에서 원인이 특정됐다: page.tsx:409의 .json 제외 필터가 past_exams.json을 걸러
  examFolderFile=null → 무한 대기. 필터 예외는 **족보 폴더에 한정**해 다른 폴더에 영향이 없게 하라.
- 스캔 이미지 썸네일이 렌더되지 않는 원인도 함께 고쳐라.
- 로딩 실패 시 무한 대기 대신 에러 메시지를 보여라.

[완료 기준]
- 연도 탭별 문항 수가 표시되고 문항 목록이 렌더된다.
- 스캔 이미지 2장이 보인다.
- 위 조사 3번의 답이 보고돼 있다.
```

---

# B그룹 — 학습 내용의 밀도

## FIX 11 — 학습노트 · 강의 요약: 텍스트 나열 → 구조

생성 로직·프롬프트·DB 스키마는 건드리지 않고 **표시 레이아웃만** 고친다.

```
[작업] 시험 대비 > 학습노트 탭과 파일 뷰 > 강의 요약 탭의 표시 레이아웃만 개선하라.
생성 로직·LLM 프롬프트·DB 스키마 변경 금지.

[현재 문제]
- 생성된 마크다운을 거의 그대로 흘려서 개념 경계가 안 보이고,
  어디가 정의이고 어디가 비유이고 어디가 시험 팁인지 구분이 안 된다.
- 본문이 화면 폭 끝까지 늘어나 읽기 어렵다.

[요구 사항]
1. '개념 1개 = 카드 1장'으로 끊어 렌더한다. 카드 사이 여백을 준다.
2. 카드 내부는 고정 순서로 렌더한다. 없는 블록은 렌더하지 않는다.
   ① 개념명 — 카드 헤더, 굵게. 우측에 기출/강조 뱃지
   ② 한 줄 정의 — 강조 배경 박스
   ③ 설명·비유 본문
   ④ 교수 인용 — 좌측 세로선 인용 블록 + 출처 칩("1주차 강의 03:38")
3. 좌측 또는 상단에 개념 목차를 두고 클릭 시 해당 카드로 스크롤.
4. 본문 폭을 제한한다(한 줄 45~75자).
5. 색·간격은 학습 가이드 화면의 카드 스타일을 기준으로 맞춘다.

[하지 말 것]
- 마크다운 원문을 수정하거나 재생성하지 마라.
- 새 라이브러리 추가 금지.

[완료 기준]
- 개념 카드가 시각적으로 분리돼 보인다.
- '대역폭' 카드에서 정의 박스 / 비유 본문 / 교수 인용 + 타임스탬프가 각각 구분된다.
- 목차 클릭 시 해당 카드로 이동한다.
- 강의 요약 탭도 같은 카드 규칙을 따른다.
- 스크린샷 한 장에 카드 2~3장이 잡히는 밀도다.
```

## FIX 12 — 슬라이드 안의 "그림"만 잘라 붙이기 (페이지 통짜 삽입 금지)

가장 좋은 학습 도해는 강의 슬라이드 안에 이미 있다. 예: 데이터통신 3주차 `(이론) 3-데이터통신-1-아날로그-소리-변환.pdf` p.7 의 2-bit/3-bit 양자화 계단 파형. 그리고 파이프라인은 개념마다 "어느 파일 몇 페이지"를 이미 근거로 저장해 두었다.

**페이지를 통째로 붙이면 안 된다.** 슬라이드에는 제목·글머리표·푸터·페이지번호가 함께 있고, 노트 본문이 이미 그 내용을 문장으로 풀어 썼다. 통으로 얹으면 같은 내용이 두 번 나오고 "슬라이드를 그냥 베끼지 않는다"는 제품 전제와 충돌한다. 필요한 건 페이지가 아니라 **그 안의 도해 하나**다.

```
[작업] 학습노트 · 강의 요약의 개념 카드에, 근거 슬라이드에서 "그림 영역만" 추출해 삽입하라.
페이지 전체 이미지를 본문에 넣지 마라. 생성 로직 변경 금지. 하드코딩 금지.

[먼저 조사해서 보고할 것]
1. 개념 ↔ 근거(파일, 페이지) 관계가 어느 테이블·컬럼에 있는지. 스키마와 샘플 행.
2. 슬라이드 리더의 페이지 렌더 경로. 재사용 가능한지 판단.
3. 표본 조사 — 데이터통신 3주차 PDF 3개에 대해 페이지별로
   `page.getOperatorList()` 에서 `OPS.paintImageXObject` / `paintJpegXObject` 를 열거하고
   **그림이 든 페이지 수 / 전체 페이지 수**를 보고하라.
   비율이 낮으면 이 작업의 효용이 작다는 뜻이므로 보고 후 내 판단을 기다려라.

[추출 방식 — 우선순위대로]
1) pdf.js operator list에서 이미지 XObject를 찾아 transform 행렬로 페이지 좌표계 bbox를 구해 그 영역만 크롭.
2) 1이 실패하고 벡터 도형인 경우: 페이지를 렌더한 뒤 텍스트 레이어의 문자 bbox를 제외하고
   남은 비백색 픽셀의 최대 연결 영역을 bbox로 잡는다.
3) 둘 다 실패하면 아무 이미지도 붙이지 않는다. 페이지 전체로 대체하지 마라.

[표시 규칙]
- 추출한 그림을 카드 안에 표시하고 아래에 캡션: "3주차 · (이론) 3-데이터통신-1-… · p.7"
- 그림이 카드 폭의 절반보다 작으면 텍스트 옆에, 크면 아래에 배치.
- 후보가 여럿이면 시험 신호가 가장 높은 페이지의 그림 1장만.
  나머지는 "관련 그림 N장 더 보기"로 접는다.
- 페이지 근거 없이 발화 시각 근거만 있는 개념은 그림 영역을 렌더하지 않는다.
- 별도로, 출처 칩 옆에 페이지 전체의 소형 썸네일(가로 60px 정도)을 둔다.
  이건 학습 자료가 아니라 출처 확인용이다. 클릭 시 슬라이드 리더 해당 페이지로 이동.

[구현 제약]
- 새 라이브러리 금지. 기존 pdf.js 경로 재사용.
- 추출 결과는 캐시한다. 카드가 화면에 들어올 때 렌더(lazy).
- 실패 시 조용히 생략. 깨진 이미지 아이콘 금지.

[완료 기준]
- '아날로그-디지털 변환' 카드에 p.7의 양자화 파형 도해만 보인다.
  제목·글머리표·푸터가 함께 들어오지 않는다.
- 글머리표만 있는 슬라이드가 근거인 개념에는 그림이 붙지 않고 빈 영역도 없다.
- 소형 썸네일 클릭 시 슬라이드 리더 해당 페이지로 이동한다.
- 그림이 붙은 개념 수 / 전체 개념 수를 보고하라.
```

## FIX 13 — 개념 구조를 도식으로 자동 생성 (LLM은 구조만, 그림은 코드가)

FIX 12로 안 되는 개념이 있다. 슬라이드가 글머리표뿐이거나(OSI 7계층을 글로만 나열), 여러 주차에 흩어진 내용을 합쳐야 하는 경우다.

**LLM에게 그림이나 마크업을 만들게 하지 말고, 정해진 스키마의 JSON만 뱉게 하고 그림은 코드가 그린다.** 기존 zod 스키마 강제 + 근거 검증 구조의 확장이다.

```
[작업] L4 학습노트·강의요약 산출에 '설명 도식' 블록을 추가한다.
LLM은 도식의 구조(JSON)만 반환하고, 그림은 코드가 인라인 SVG로 그린다.

[허용 타입 — 이 6종만. 그 외는 스키마에서 거부한다]
  layers   : { type:'layers',  title, layers:[{name, note}] }              // 위→아래 순서 유지
  compare  : { type:'compare', title, labelA, labelB, rows:[{axis, a, b}] }
  flow     : { type:'flow',    title, steps:[{label, note}] }
  parts    : { type:'parts',   title, whole, parts:[{label, note, size?}] }
  sequence : { type:'sequence',title, actors:[String], messages:[{from, to, label}] }
  matrix   : { type:'matrix',  title, cols:[String], rows:[{label, cells:[String]}] }

[규칙]
1. zod로 강제하고 파싱 실패 시 재시도한다(기존 엔진 어댑터 방식 그대로).
2. LLM이 생성한 HTML·SVG·마크다운 표는 절대 그대로 렌더하지 마라. JSON만 받는다.
3. 근거 검증: 도식에 등장한 항목 이름(layers[].name, parts[].label, actors 등)이
   해당 개념의 근거(슬라이드 텍스트 또는 전사)에 실제로 존재하는지 확인한다.
   과반이 확인되지 않으면 반려하고 노출하지 않는다. 반려 건수를 실행 로그에 남긴다.
4. 모든 개념에 도식을 만들지 마라. 6종 중 하나에 명확히 해당하는 개념에만 붙이고,
   애매하면 도식 없이 텍스트만 낸다. "억지로 하나 만들기 금지"를 프롬프트에 명시하라.
5. FIX 12로 그림이 붙은 개념에는 도식을 만들지 않는다. 원본 그림이 항상 우선이다.

[렌더 규칙 — 코드]
- 인라인 SVG 또는 CSS grid. 새 차트 라이브러리 금지.
- 앱의 기존 색 토큰만 사용. 도식마다 색을 새로 만들지 마라.
- 폭이 좁아지면 세로로 접힌다.
- 도식 하단에 캡션과 출처 칩을 단다.

[완료 기준]
- 'OSI 7계층'에서 layers 도식이 7단 스택으로 렌더된다.
- '아날로그 vs 디지털'에서 compare 도식이 표로 렌더된다.
- 'ADC'에서 flow 도식이 샘플링→양자화→부호화로 렌더된다.
- 검증 실패 도식은 화면에 없고, 반려 건수를 로그에서 확인할 수 있다.
- 도식 없는 개념은 기존대로 텍스트만 나온다. 빈 영역이 없다.
- 같은 자료로 두 번 실행했을 때 도식 타입이 뒤바뀌지 않는지 확인해 보고하라.
```

---

# C그룹 — 저비용 정합성

## FIX 5 — 모의고사에 답 입력칸 추가

```
[작업] 시험 대비 > 모의고사에 답 입력 단계를 추가하라. 문제 생성 로직 변경 금지.

[현재] 문제 → [정답 확인] → 정답 노출 → "스스로 채점하세요(맞음/틀림)"
[문제] 파일 뷰 > 퀴즈 > 연습 모드에는 답 입력 textarea가 이미 있다.
      더 가벼운 '연습'에 입력칸이 있고 더 무거운 '모의고사'에 없는 역전 상태다.

[요구 사항]
1. 연습 모드의 답 입력 컴포넌트를 찾아 보고하고 그것을 재사용하라. 새로 만들지 마라.
2. 흐름: 문제 → 답 입력(textarea) → [제출] → 정답·해설·출처 노출 → 자기채점(맞음/틀림)
3. 입력 전에는 [정답 확인] 버튼이 보이지 않아야 한다.
4. 마지막 문항 제출 후 결과 요약: 총 문항 / 맞음 / 틀림 / 틀린 문항의 개념 목록.
5. 결과는 세션 내 상태로만 유지해도 된다. 학습 이력 저장 기능은 만들지 마라.

[완료 기준]
- 1번 문항에서 입력칸이 보이고 입력 전 정답이 노출되지 않는다.
- 제출 후 정답·해설·출처가 함께 보인다.
- 전 문항 종료 후 결과 요약이 뜬다.
```

## FIX 6 — 첫 화면을 홈으로

```
[작업] 앱 진입 시 기본 화면을 '모든 문서'(파일 탐색기)에서 '홈'으로 바꿔라.
라우트 기본값만 바꾼다. 두 화면의 내용은 수정하지 마라.

[완료 기준]
- 앱/웹 진입 시 홈(시간표 · D-day · 다가오는 시험 · 이번 주 과제)이 먼저 뜬다.
- 좌측 '모든 문서' 경로로 여전히 접근 가능하다.
```

## FIX 7 — 과목 카드의 개념 수: 표시할 "값"을 고쳐라 (라벨 문제가 아니다)

```
[작업] 개념 수 표기가 화면마다 다른 문제를 고쳐라.

[확인된 사실]
- 시험 선택 화면: 데이터통신 "8개 수업 분석됨 · 개념 20개",
                운영체제 "8개 수업 분석됨 · 개념 20개" — 두 과목이 같은 20이다.
- 학습 가이드 배너: "에이전트가 학기 전체 자료에서 개념 92개를 뽑고 공부 순서까지 정리했어요"
- 그 아래 카드: "개념 20개 · 기출 36문항"
- 실제 개념 수는 데이터통신 92개, 운영체제 86개다.

즉 저 20은 과목의 개념 수가 아니라 가이드에 싣는 상위 N 고정값인데,
그것을 과목 지표인 것처럼 표시하고 있다. 라벨이 아니라 값 선택이 잘못됐다.

[먼저 조사할 것]
과목별 전체 개념 수를 반환하는 쿼리와, 가이드 상위 N을 결정하는 코드 위치를 보고하라.

[요구 사항]
1. 시험 선택 화면의 과목 카드에는 과목 전체 개념 수를 표시한다(데이터통신 92, 운영체제 86).
2. 학습 가이드 안의 목록 헤더만 "이번 시험 핵심 20개 (전체 92개 중)" 형식으로 표기한다.
3. 배너 문구의 92와 카드의 숫자가 같은 대상을 가리키도록 맞춘다.

[완료 기준]
- 두 과목의 과목 카드 숫자가 서로 다르고, 각각 DB 조회 결과와 일치한다.
- 한 화면 안에서 92와 20이 동시에 나올 때 각각이 무엇인지 문구로 구분된다.
```

## FIX 8 — 탭 라벨 구분 + 폴더 기본 탭

```
[작업] 폴더(주차) 뷰와 파일 뷰의 탭 라벨을 구분하고, 폴더 뷰 기본 탭을 바꿔라.
구조는 그대로 둔다. 탭을 옮기거나 합치지 마라.

[문제]
- 폴더 뷰 탭: 자료 / 강의 요약 / 퀴즈
- 파일 뷰 탭: 슬라이드 설명 / 강의 요약 / 퀴즈 / 녹음·필기 / AI 챗봇
  → "강의 요약", "퀴즈"가 양쪽에 같은 이름으로 있어서,
    학생이 지금 보는 게 3주차 전체인지 이 PDF 하나인지 화면이 말해주지 않는다.
- 폴더 뷰 기본 탭이 '자료'(파일 목록)라, 3주차를 눌렀는데 파일 탐색기를 먼저 만난다.

[요구 사항]
1. 폴더 뷰 탭 라벨: "3주차 전체 요약", "3주차 전체 퀴즈" (폴더명을 동적으로 넣는다)
2. 파일 뷰 탭 라벨: "이 자료 요약", "이 자료 퀴즈"
3. 폴더 뷰 기본 탭을 '강의 요약'으로 바꾼다. '자료' 탭은 그대로 둔다.

[완료 기준]
- 3주차를 누르면 '3주차 전체 요약'이 먼저 뜬다.
- 파일을 열면 탭이 '이 자료 …'로 표시된다.
- 탭 개수와 순서는 이전과 동일하다.
```

## FIX 9 — 과목 트리에 시간표의 전 과목 노출

```
[작업] 좌측 과목 트리에 시간표에서 인식된 과목을 전부 표시하라.

[확인된 사실 — 오해하지 마라]
앱은 이미 8과목을 알고 있다. 홈 '다가오는 시험'에 데이터베이스 D-13이 있고,
시험 대비 화면에도 데이터베이스 카드가 "아직 분석된 수업이 없어요" 상태로 존재한다.
끊긴 건 시간표 인식이 아니라, 좌측 트리가 자료가 있는 과목만 렌더한다는 점이다.

[요구 사항]
1. 시간표에서 인식된 과목을 전부 트리에 표시한다.
2. 자료가 없는 과목은 "자료 없음" 빈 상태로 표시하고, 클릭 시 자료를 넣으라는 안내를 보여준다.
3. 자료가 있는 과목과 없는 과목이 시각적으로 구분되게 한다(흐리게 등).

[완료 기준]
- 트리에 데이터통신·운영체제 외 시간표의 나머지 과목이 보인다.
- 빈 과목을 눌러도 에러 없이 안내 화면이 뜬다.
```

## FIX 10 — 파일 뷰 탭을 바꿔도 PDF 리더가 닫히지 않게

```
[작업] 파일 뷰에서 탭을 전환할 때 PDF 리더가 통째로 닫히는 문제를 고쳐라.

[증상]
'슬라이드 설명' 탭에서 PDF를 보다가 강의 요약 / 퀴즈 / 녹음·필기 / AI 챗봇 중
아무 탭이나 누르면 리더 전체가 닫히고 레이아웃이 바뀐다.
녹음·필기만의 문제가 아니라 전 탭에서 발생한다.

[요구 사항]
1. 탭을 전환해도 파일 컨텍스트(어떤 PDF, 몇 페이지였는지)를 유지한다.
2. 가능하면 리더를 좌측에 유지한 채 우측 패널만 탭 내용으로 교체한다.
   레이아웃 재설계가 필요하다면 하지 말고, 최소한 '슬라이드 설명'으로 돌아왔을 때
   보던 페이지가 복원되게만 하라.
3. 탭 바의 화면상 위치가 탭마다 달라지지 않게 한다(현재 리더가 닫히면서 위아래로 점프한다).

[완료 기준]
- p.7을 보다가 강의 요약 → 슬라이드 설명으로 돌아오면 p.7이다.
- 탭 전환 시 탭 바 위치가 움직이지 않는다.
```

---

# D그룹 — 여유가 될 때

## FIX 14 — 전사본 뷰어 (타임스탬프 앵커)

이건 두 문제를 한 번에 해결한다. 페이지 근거는 이미 클릭돼서 슬라이드로 이동하는 것을 확인했다. 죽어 있는 건 발화 시각 근거뿐이고, 이유는 전사본을 타임스탬프로 볼 화면이 없기 때문이다.

```
[작업] 전사본 뷰어를 만들고, 발화 시각 근거를 클릭 가능하게 연결하라.

[현재]
- 녹음·필기 탭은 transcript.txt 파일 한 줄이 전부다.
- 개념 카드의 "근거 · 1주차 03:38"은 회색 텍스트이고 클릭되지 않는다.
- 반면 페이지 근거(강의 요약의 페이지 칩)는 이미 슬라이드 리더로 이동한다. 그 패턴을 따르라.

[요구 사항]
1. 녹음·필기 탭에서 전사본을 타임스탬프 단위 블록으로 렌더한다(파일 목록이 아니라 본문).
2. 각 블록에 시각 앵커를 단다. URL 또는 스크롤 앵커로 특정 시각으로 이동 가능하게 한다.
3. 개념 카드의 "근거 · 1주차 03:38"을 링크로 바꾸고, 클릭 시 해당 전사 블록으로 이동 + 하이라이트.
4. 교수 강조로 표시된 발화는 시각적으로 구분한다.

[완료 기준]
- 개념 카드의 발화 시각 근거를 클릭하면 전사본의 해당 위치로 이동하고 그 블록이 강조된다.
- 전사본에서 강조 발화가 일반 발화와 구분돼 보인다.
```

## FIX 15 — 폴더 순서를 눈에 보이게 (드래그 재정렬은 만들지 말 것)

```
[작업] 폴더 순서가 어떻게 정해졌는지 화면에 드러나게 하라. 재정렬 UI는 만들지 마라.

[배경]
folders.ts 의 `unitOrder: m ? parseInt(m[1],10) : 1000 + mtimeRank` —
폴더명에 숫자가 있으면 그 숫자가 순서, 없으면 1000번대로 밀려 폴더 생성 시각 순이 된다.
이 파이프라인은 "먼저 배운 개념이 선수"라는 가정 위에 서 있으므로,
폴더 순서가 틀리면 학습 경로 전체가 틀린다. 그런데 지금은 그 실패가 조용하다.

[요구 사항]
1. 과목 트리의 각 폴더 앞에 순서 번호를 표시한다.
2. 폴더명에서 숫자를 못 찾아 생성 시각으로 순서가 정해진 폴더에는 "순서 미지정" 배지를 단다.
3. 배지 툴팁: "이 폴더는 순서를 알 수 없어 만든 순서대로 배치됩니다.
   폴더 이름에 회차 번호를 넣으면 학습 경로가 정확해집니다."

[하지 말 것]
- 드래그 재정렬 UI를 만들지 마라.
- unitOrder 계산 로직을 바꾸지 마라. 표시만 한다.

[완료 기준]
- 트리에 1, 2, 3… 순서가 보인다.
- 숫자 없는 이름의 폴더를 만들면 "순서 미지정" 배지가 뜬다.
```
